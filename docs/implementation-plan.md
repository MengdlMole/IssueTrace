# IssueTrace 0.5 实施状态

## 已完成

- [x] 产品收敛为事件管理、时间线、计时和停留提醒；不引入复杂大盘。
- [x] 事件管理/事件记录/事件日历三页面导航，固定时间线输入、倒序虚拟列表和可滚动长记录。
- [x] 日历按提出日期聚合月视图和单日事件列表，并可直接跳转到对应事件记录。
- [x] 快速创建补充分组、标签、版本、服务、处理人和优先级；与详情页复用字段组件。
- [x] 分组、提出人、处理人、版本和服务支持历史值下拉选择及自由录入。
- [x] 管理页左侧多级分组树/右侧事件结果，以及记录页左侧关注与普通事件/右侧编辑区。
- [x] 记录页事件列表的优先级筛选，以及最近修改、创建时间和优先级排序。
- [x] 分组树统一缩进、持久化拖动排序，以及父分组和全部后代的事务式路径迁移。
- [x] 管理页全文搜索、服务/版本/跟踪单筛选、事件卡片标签多选和五种排序。
- [x] 记录页“需要关注/全部事件”可调区域，以及标题、描述、进展、标签共用搜索。
- [x] 顶栏使用单一页面状态驱动的分段页签，避免原生焦点造成双高亮。
- [x] 事件描述文字、粘贴图片、选择图片和拖放图片的一体化编辑区。
- [x] 累计计时范围筛选，以及多级分组和多标签规范化。
- [x] 暂停计时后可按小时和分钟修正累计处理时间，运行中禁止覆盖。
- [x] 收件箱、时间线输入、时间线列表和问题详情组件化，控制器通过显式 QML 单例注册。
- [x] 问题切换、状态修改、详情关闭和应用退出前刷新待保存内容。
- [x] 四状态模型，以及旧 `verifying/resolved/closed` 状态迁移。
- [x] 持久化状态变更时间和显式提醒时间。
- [x] 问题年龄、状态停留、未活动和“需要关注”投影。
- [x] 30 分钟、2 小时、明天此时和自定义未来时间提醒，桌面通知和系统托盘。
- [x] 快捷提交、按问题保存草稿、粘贴截图、拖放和批量附件。
- [x] Markdown 自包含导出、当前筛选 XLSX 导出。
- [x] 总结模板提供独立“验证和遗留问题”章节，不把验证内容混入解决章节。
- [x] Markdown/XLSX 导出从 UI 控制器拆分为可独立测试的应用服务。
- [x] schema 8 自动迁移、迁移前快照、工作区验证/备份/恢复。
- [x] 完整压缩包离线升级、健康检查和回滚。
- [x] Windows 11 原生 PowerShell 便携构建、部署、归档和升级烟测脚本。

## 发布验证

- `issuetrace_core_tests`：CRUD、状态/提醒持久化、附件、搜索、备份、迁移和 XLSX；
- `issuetrace_form_template_tests`：默认补充信息表单的解析与校验；
- `issuetrace_issue_export_service_tests`：中文路径、附件链接、状态映射、Markdown/XLSX 和覆盖保护；
- `issuetrace_update_package_tests`：清单、哈希、版本和归档边界；
- `issuetrace_updater_integration`：升级成功与失败回滚；
- `issuetrace_ui_scroll_smoke`：创建长时间线并验证视口可滚动；
- `issuetrace_ui_status_refresh_smoke`：点击状态按钮并验证详情按钮、当前问题和左侧投影即时一致；
- `issuetrace_custom_reminder_smoke`：验证自定义未来时间保存和非法时间拒绝；
- `issuetrace_description_image_smoke`：验证详情图片入口可见、图片入库和前端投影；
- `issuetrace_two_pane_navigation_smoke`：验证记录页左栏存在、编辑列表不受管理筛选影响、分组整体迁移、历史字段投影、日历数据，以及三个顶栏页签互斥高亮；
- sanitizer 核心测试；
- macOS 原生便携包烟测和 Windows 交叉构建依赖闭包。
- 应用图标、macOS bundle 元数据与 Windows 文件版本资源。

## 暂缓

1.0.0 的定位是内部预览版。真实 Windows 11、Linux X11/Wayland 的人工 GUI 验收，以及 macOS 签名/公证，必须在对应发布环境执行；未完成前发布物保持 `-dev` 后缀。产品名称、域名和应用标识符也要在公开发布前最终确认。

团队 XLSX、双向 Obsidian、AI 总结和复杂大盘不在当前路线图；先用 0.5 的日常使用反馈判断是否确有必要。
